spring:
  application:
    name: Incident
  datasource:
    url: jdbc:mysql://roundhouse.proxy.rlwy.net:42973/railway
    username: root
    password: qHRVmzZdIPTUHBlalCcGMKIPlBMUWhUx
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

